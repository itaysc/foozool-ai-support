[build]
builder = "dockerfile"

[deploy]
healthcheckPath = "/api/v1/health/database"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
memory = "1GB"

[variables]
ENVIRONMENT = "production"
RAILWAY_ENVIRONMENT = "production" 