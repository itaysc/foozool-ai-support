# Ultra-minimal requirements - web framework only
fastapi==0.115.8
uvicorn==0.34.0
pydantic==2.10.6
gunicorn==21.2.0

# Essential utilities only
python-json-logger==2.0.7
prometheus-client==0.17.1

# Compatible NumPy version to avoid conflicts with PyTorch
numpy<2.0.0

# NO ML dependencies - they will be installed at runtime 