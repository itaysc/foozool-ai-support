# Machine Learning Dependencies - Optimized for size
torch==2.1.0  # Standard version, compatible with Debian
transformers==4.35.0
sentence-transformers==2.2.2
tensorflow==2.15.0  # Required for Sarthak279/Intent model

# Use smaller, more efficient alternatives
# Consider replacing large models with smaller ones:
# - facebook/bart-large-cnn -> facebook/bart-base-cnn (smaller)
# - all-mpnet-base-v2 -> all-MiniLM-L6-v2 (already included, smaller)
# - vineetsharma/customer-support-intent-albert -> distilbert-base-uncased (smaller)

# Optional: Add specific model versions if needed
# Example:
# transformers[sentencepiece]==4.35.0 